# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/

# IDEs
.vscode/
.claude/

# MCP server config (local)
.mcp.json

# Phase-A variant cache (two-phase run) — a pure function of the variation
# seeds, rebuilt on demand. See docs/PARALLELIZATION_PLAN.md.
.variant_cache/

# Generated outputs
convergence_plot*.png
root_influence_analysis*.png
two_phase_dynamics*.png

# API keys and secrets
.env
*.key
secrets.json
config_local.py

# ---------------------------------------------------------------------------
# Publication boundary — local-only, never tracked.
# See PUBLICATION_CHECKLIST.md for the rationale behind each exclusion.
# ---------------------------------------------------------------------------
# Manuscript drafts; the published article is reached via its DOI.
*.pdf
manuscript/
slides/

# NOTE: networks/citation_data/*_works.pkl are deliberately TRACKED, not ignored.
# They were briefly untracked on 2026-08-26 and re-tracked the same day: the raw
# OpenAlex dumps are an April-2026 snapshot that a fresh API query no longer
# reproduces, and untracking them made a collaborator's first pull delete their
# local copy. See PUBLICATION_CHECKLIST.md before changing this.

# ---------------------------------------------------------------------------
# Methodological groundwork kept local — not part of the published companion
# code. Untracked 2026-08-28; still reachable in history before that commit.
# ---------------------------------------------------------------------------
model/convergence_analysis/

# Tooling caches
.pytest_cache/
.ruff_cache/

model/convergence_analysis/

utils/graph_pipeline/
