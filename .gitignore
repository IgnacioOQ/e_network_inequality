# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/

# IDEs
.vscode/
.claude/

# MCP server config (local)
.mcp.json

# Generated outputs
convergence_plot*.png
root_influence_analysis*.png
two_phase_dynamics*.png

# API keys and secrets
.env
*.key
secrets.json
config_local.py

# ---------------------------------------------------------------------------
# Publication boundary — local-only, never tracked.
# See PUBLICATION_CHECKLIST.md for the rationale behind each exclusion.
# ---------------------------------------------------------------------------
# Manuscript drafts; the published article is reached via its DOI.
*.pdf
manuscript/
slides/

# Raw OpenAlex dumps — regenerated by `1. Citation Data and Networks Generation.ipynb`.
# ~150 MB of API responses; the derived networks (*_network.pkl) are tracked instead.
networks/citation_data/*_works.pkl

# Tooling caches
.pytest_cache/
.ruff_cache/
